# План реализации 3-го этапа: Запись на собеседования

## 📋 Обзор
Реализация функционала управления слотами собеседований для админов и проверяющих через Mini App.

---

## 🎯 Этап 1: Backend API - Управление слотами

### 1.1. Создать `app/api/routers/interview_slots.py`

**Эндпоинты:**

#### Для Head Admin (создание/редактирование слотов):
- `GET /api/v1/interview-slots/{faculty_id}` - список всех слотов факультета
- `POST /api/v1/interview-slots/{faculty_id}` - создать слот
  - Параметры: дата, время начала/окончания, количество мест, локация
- `PUT /api/v1/interview-slots/{slot_id}` - обновить слот
- `DELETE /api/v1/interview-slots/{slot_id}` - удалить слот

#### Для Reviewer (настройка занятости):
- `GET /api/v1/interview-slots/{faculty_id}/my-availability` - мои слоты занятости
- `POST /api/v1/interview-slots/{slot_id}/availability` - отметить занятость
- `DELETE /api/v1/interview-slots/{slot_id}/availability` - снять занятость

**Схемы Pydantic:**
```python
class InterviewSlotCreate(BaseModel):
    datetime_start: datetime
    datetime_end: datetime
    max_participants: int = 1
    location: str | None = None

class InterviewSlotResponse(BaseModel):
    id: int
    datetime_start: datetime
    datetime_end: datetime
    max_participants: int
    location: str | None
    current_participants: int
    available_places: int
    created_by: int | None
    created_at: datetime

class SlotAvailability(BaseModel):
    slot_id: int
    available: bool  # свободен/занят
```

**Логика:**
- Head Admin может создавать слоты с количеством мест (`max_participants`)
- Reviewer может отмечать свою занятость/свободность в слотах
- При создании слота Head Admin автоматически становится доступным

---

## 🎨 Этап 2: Frontend - Страница управления слотами

### 2.1. Создать HTML-структуру в `frontend/index.html`

**Новый экран: `interview-slots-admin`** (для Head Admin):
```html
<div id="interview-slots-admin" class="admin-screen hidden">
    <div class="admin-header">
        <h2>📅 Управление слотами собеседований</h2>
        <button class="btn btn-primary" onclick="showCreateSlot()">+ Создать слот</button>
    </div>
    <div id="slots-list"></div>
</div>
```

**Новый экран: `interview-slots-reviewer`** (для Reviewer):
```html
<div id="interview-slots-reviewer" class="admin-screen hidden">
    <div class="admin-header">
        <h2>📅 Моя занятость</h2>
        <p class="hint-text">Отметьте слоты, в которые вы свободны для проведения собеседований</p>
    </div>
    <div id="availability-list"></div>
</div>
```

**Модальное окно создания слота:**
```html
<div id="create-slot-modal" class="modal hidden">
    <div class="modal-content">
        <h3>Создать слот собеседования</h3>
        <form id="create-slot-form">
            <input type="date" id="slot-date" required>
            <input type="time" id="slot-time-start" required>
            <input type="time" id="slot-time-end" required>
            <input type="number" id="slot-max-participants" min="1" value="1" required>
            <input type="text" id="slot-location" placeholder="Локация (опционально)">
            <div class="modal-actions">
                <button type="submit" class="btn btn-primary">Создать</button>
                <button type="button" class="btn btn-secondary" onclick="closeCreateSlotModal()">Отмена</button>
            </div>
        </form>
    </div>
</div>
```

### 2.2. JavaScript функции в `frontend/app.js`

**Для Head Admin:**
```javascript
async function loadInterviewSlots() {
    // GET /api/v1/interview-slots/{faculty_id}
    // Отображение списка слотов
}

async function createSlot(slotData) {
    // POST /api/v1/interview-slots/{faculty_id}
}

async function updateSlot(slotId, slotData) {
    // PUT /api/v1/interview-slots/{slot_id}
}

async function deleteSlot(slotId) {
    // DELETE /api/v1/interview-slots/{slot_id}
}
```

**Для Reviewer:**
```javascript
async function loadMyAvailability() {
    // GET /api/v1/interview-slots/{faculty_id}/my-availability
    // Отображение слотов с возможностью отметить занятость
}

async function toggleAvailability(slotId, available) {
    // POST/DELETE /api/v1/interview-slots/{slot_id}/availability
}
```

### 2.3. Интеграция в Mini App

**Добавить в `init()`:**
```javascript
// После проверки прав админа
if (adminCheck.is_admin) {
    if (adminCheck.role === 'head_admin') {
        // Показываем кнопку "Управление слотами" в админ-панели
    } else if (adminCheck.role === 'reviewer') {
        // Показываем кнопку "Моя занятость" в админ-панели
    }
}
```

---

## 🔧 Этап 3: Расширение модели InterviewSlot (если нужно)

**Возможные дополнения:**
- Поле `interviewer_id` - какой проверяющий назначен на слот
- Отдельная таблица `SlotAvailability` для отслеживания занятости проверяющих
- Поле `is_active` - активен ли слот

**Или использовать существующую модель:**
- `created_by` - кто создал слот (Head Admin)
- Отдельная таблица связи многие-ко-многим для доступности слотов проверяющим

---

## 📱 Этап 4: UI/UX компоненты

### 4.1. Календарь слотов
- Отображение слотов по датам
- Цветовая индикация: свободные/занятые/переполненные
- Фильтры по датам

### 4.2. Форма создания слота
- Выбор даты и времени
- Валидация (время начала < времени окончания)
- Возможность создать несколько слотов одним действием

### 4.3. Управление занятостью (для Reviewer)
- Чекбоксы/переключатели для каждого слота
- Быстрое выделение диапазона слотов
- Визуальная индикация выбранных слотов

---

## 🗄️ Этап 5: База данных (если нужно расширение)

**Возможная новая таблица:**
```sql
CREATE TABLE slot_availability (
    id SERIAL PRIMARY KEY,
    slot_id INTEGER REFERENCES interview_slots(id) ON DELETE CASCADE,
    interviewer_id INTEGER REFERENCES administrators(id) ON DELETE CASCADE,
    available BOOLEAN DEFAULT true,
    created_at TIMESTAMP DEFAULT NOW(),
    UNIQUE(slot_id, interviewer_id)
);
```

**Или использовать существующую структуру:**
- Поле `created_by` в `InterviewSlot` для связи с Head Admin
- Отдельное поле или таблица для отметки доступности Reviewer

---

## 📊 Этап 6: Валидация и бизнес-логика

### 6.1. Валидация слотов:
- Время начала < времени окончания
- Слоты не должны пересекаться (для одного факультета)
- Максимальное количество мест > 0
- Дата не в прошлом

### 6.2. Логика занятости:
- Reviewer может отмечать/снимать свою занятость
- Нельзя удалить слот, на который уже записались пользователи
- При удалении слота отменяются все связанные записи

---

## 🚀 Этап 7: Интеграция с Mini App

### 7.1. Добавить в админ-панель:
- Кнопку "📅 Слоты собеседований" для Head Admin
- Кнопку "📅 Моя занятость" для Reviewer

### 7.2. Навигация:
- Переход между экранами
- Кнопка "Назад" в админ-панель

---

## 📝 Этап 8: Тестирование

- Создание слотов Head Admin
- Отметка занятости Reviewer
- Валидация данных
- Проверка прав доступа
- Обработка ошибок

---

## 🎯 Приоритеты реализации

1. **Высокий приоритет:**
   - Backend API для создания/просмотра слотов (Head Admin)
   - Backend API для настройки занятости (Reviewer)
   - Frontend страница управления слотами (Head Admin)
   - Frontend страница настройки занятости (Reviewer)

2. **Средний приоритет:**
   - Редактирование/удаление слотов
   - Валидация пересечений слотов
   - Календарное отображение

3. **Низкий приоритет:**
   - Массовое создание слотов
   - Экспорт расписания
   - Уведомления о новых слотах

---

## ❓ Вопросы для уточнения

1. Нужна ли отдельная таблица для отслеживания занятости Reviewer или использовать существующую структуру?
2. Может ли Reviewer видеть, кто ещё свободен в слот?
3. Нужно ли ограничение на количество слотов в день?
4. Может ли Head Admin редактировать слоты, созданные другими админами?
5. Что происходит со слотами, когда этап собеседований закрывается?

---

## 🎨 Предлагаемая структура файлов

```
app/api/routers/
  └── interview_slots.py  # Новый файл с API для управления слотами

frontend/
  ├── index.html          # Добавить новые экраны
  ├── app.js              # Добавить функции управления слотами
  └── styles.css          # Добавить стили для новых экранов
```

---

**Готов начать реализацию! С чего начнём?** 🚀
