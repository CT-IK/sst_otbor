# Структура БД для этапа "Запись на собеседования"

## 📋 Обзор

Система позволяет:
- **Head Admin** создавать слоты собеседований (дата, время, количество мест, локация)
- **Reviewer** отмечать свою доступность/занятость в слотах
- Пользователям записываться на собеседования

---

## 🗄️ Таблицы

### 1. `interview_slots` (обновлена)

**Назначение:** Слоты собеседований, созданные Head Admin'ом

**Поля:**
- `id` - первичный ключ
- `faculty_id` - ID факультета (FK → faculty.id)
- `datetime_start` - дата и время начала слота
- `datetime_end` - дата и время окончания слота
- `max_participants` - максимальное количество участников (по умолчанию 1)
- `location` - локация (аудитория/ссылка, опционально)
- `created_by` - кто создал слот (FK → administrators.id, Head Admin)
- **`is_active`** ⭐ (новое) - активен ли слот (можно деактивировать без удаления)
- `created_at` - дата создания

**Relationships:**
- `faculty` → Faculty
- `creator` → Administrator (кто создал)
- `interviews` → Interview[] (записи пользователей)
- `available_interviewers` → SlotAvailability[] (кто доступен)

---

### 2. `slot_availability` (новая) ⭐

**Назначение:** Связь многие-ко-многим между слотами и проверяющими.
Reviewer'ы отмечают свою доступность в слотах.

**Поля:**
- `id` - первичный ключ
- `slot_id` - ID слота (FK → interview_slots.id, CASCADE)
- `interviewer_id` - ID проверяющего (FK → administrators.id, CASCADE)
- `available` - доступен ли проверяющий (true = свободен, false = занят)
- `created_at` - когда отметили доступность
- `updated_at` - когда последний раз обновляли

**Ограничения:**
- UNIQUE(slot_id, interviewer_id) - один проверяющий не может иметь несколько записей в один слот

**Relationships:**
- `slot` → InterviewSlot
- `interviewer` → Administrator

**Логика:**
- Head Admin создает слот → может автоматически отметить себя как доступного
- Reviewer видит список слотов → отмечает галочками, где он свободен
- При записи пользователя выбирается один из доступных проверяющих

---

### 3. `interviews` (обновлена)

**Назначение:** Записи пользователей на собеседования и результаты

**Поля:**
- `id` - первичный ключ
- `user_id` - ID пользователя (FK → users.id, CASCADE)
- `faculty_id` - ID факультета (FK → faculty.id, CASCADE)
- `slot_id` - ID слота (FK → interview_slots.id, SET NULL)
- **`interviewer_id`** ⭐ (новое) - назначенный проверяющий (FK → administrators.id, SET NULL)
- `status` - статус собеседования (scheduled/completed/no_show/cancelled)
- `interviewer_notes` - заметки проверяющего
- `score` - оценка (1-10)
- `created_at` - дата записи
- `completed_at` - дата завершения

**Relationships:**
- `user` → User
- `faculty` → Faculty
- `slot` → InterviewSlot
- `interviewer` → Administrator (кто проводит)

**Логика:**
- При записи пользователя выбирается слот (`slot_id`)
- При назначении проверяющего проставляется `interviewer_id`
- `interviewer_id` может быть NULL, если проверяющий ещё не назначен

---

## 🔄 Логика работы

### Создание слота (Head Admin):
1. Head Admin создает слот через API
2. Слот сохраняется в `interview_slots`
3. Head Admin может автоматически отметить себя как доступного в `slot_availability`

### Отметка доступности (Reviewer):
1. Reviewer видит список слотов факультета
2. Отмечает галочками, где он свободен
3. Создаются/удаляются записи в `slot_availability`
4. `available = true` - свободен, `false` - занят

### Запись пользователя:
1. Пользователь выбирает слот (если есть свободные места)
2. Создается запись в `interviews` с `slot_id`
3. При назначении проверяющего проставляется `interviewer_id`
4. Проверяющий выбирается из доступных в этом слоте (`slot_availability`)

---

## 📊 Индексы

Для оптимизации созданы индексы:
- `idx_slot_availability_slot_id` - быстрый поиск по слоту
- `idx_slot_availability_interviewer_id` - быстрый поиск по проверяющему
- `idx_slot_availability_available` - фильтрация по доступности
- `idx_interview_slots_faculty_id` - слоты по факультету
- `idx_interview_slots_datetime_start` - сортировка по дате
- `idx_interview_slots_is_active` - фильтрация активных слотов
- `idx_interviews_interviewer_id` - собеседования по проверяющему
- `idx_interviews_slot_id` - собеседования по слоту

---

## 🚀 Миграция

Для применения изменений выполните:
```bash
docker compose -f docker-compose.prod.yml exec postgres psql -U sst_user -d sst_db -f /path/to/migration/add_interview_slots_structure.sql
```

Или напрямую:
```bash
cat migration/add_interview_slots_structure.sql | docker compose -f docker-compose.prod.yml exec -T postgres psql -U sst_user -d sst_db
```

---

## ✅ Чеклист для проверки

- [x] Модель `InterviewSlot` обновлена (`is_active`)
- [x] Модель `Interview` обновлена (`interviewer_id`)
- [x] Модель `SlotAvailability` создана
- [x] Relationships настроены
- [x] SQL миграция создана
- [ ] Миграция применена на сервере
- [ ] Индексы созданы
